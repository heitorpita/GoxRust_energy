# analise/analise.ipynb — versoes usadas para gerar o relatorio
# instalar:  pip install -r requirements.txt   (ou: uv pip install -r requirements.txt)

pandas>=3.0.6
numpy>=2.5.3
matplotlib>=3.11.2
seaborn>=0.13.2
jupyter>=1.1.1
tabulate>=0.10.0
